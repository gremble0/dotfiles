[user]
  email = herman.g.stornes@indra.no
  name = Herman Gard Stornes
[pull]
  rebase = false
[push]
  autoSetupRemote = true
[init]
  defaultBranch = main
[color]
  pager = yes
[alias]
  st = status
  # Commit
  cm = commit
  ca = commit --amend
  lg = log --name-status
  cp = cherry-pick
  ad = add
  # Remote
  rv = remote -v
  fe = fetch
  pu = push
  pl = pull
  cl = clone
  # Diffs
  df = diff
  dc = diff --cached
  bl = blame
  # Branches
  co = checkout
  br = branch
  sw = switch
  rb = rebase
  mg = merge
  # Stash
  sp = stash pop
  ss = stash
  # Worktree
  wl = worktree list
[core]
  editor = nvim
