#!/bin/sh
# Script to increment brightness by 10%

current=$(xrandr --verbose --current | grep ^"eDP-1" -A5 | tail -n1 | cut -d ":" -f 2)
new="$(echo "$current + 0.1" | bc)"
echo "Increased brightness to $new"
xrandr --output $screenname --brightness $new
